// Page-specific logic for index.html (body has data-page="home")
export function initHome() {
  console.log("✅ home page module loaded");
  // Hero + category grid rendering will be added in the next step
}
